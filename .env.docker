# Docker Environment Configuration for CHIRP Radio

# ============================================
# CMS API Configuration
# ============================================
VITE_USE_CMS_API=true
# Use relative path - nginx proxies to CMS
VITE_CMS_API_URL=/api

# Note: All database access is handled through the CMS API.
# The frontend does not connect directly to PostgreSQL.
