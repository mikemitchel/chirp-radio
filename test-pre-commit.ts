// Test file with formatting issues
const foo = 'bar' // Extra spaces and semicolon
const baz = { a: 1, b: 2 } // No spaces in object

export const testFunction = () => {
  console.log(foo, baz)
  return true
}
